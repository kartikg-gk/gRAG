# Personal access token used for GitHub ingestion.
# Public repositories work without one, but the unauthenticated rate limit is
# 60 requests/hour, which is not enough for a real ingest.
GITHUB_TOKEN=
